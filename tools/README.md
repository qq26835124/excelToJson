# 使用说明

> 命令行执行如下命令：

```sh
  npm run excelToJson [source] [targetName] [path]
  # source 是将要转换成json的excel文件，必选参数
  # targetName 转换成功后的json文件名，可选（默认名为output.json）
  # path 转换成功后的json文件所在目录，可选（默认当前目录）
  # eg. npm run excelToJson example.xls example
```